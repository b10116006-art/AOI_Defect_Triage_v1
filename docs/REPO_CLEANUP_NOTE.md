# REPO_CLEANUP_NOTE.md — AOI_Defect_Triage_v1

_Written: 2026-04-06_  
_Purpose: track recommended cleanup actions. No files are deleted automatically._

---

## Recommended repo structure

```
AOI_Defect_Triage_v1/
├── .gitignore
├── requirements.txt
├── src/
│   ├── prepare_data.py
│   ├── train.py
│   ├── evaluate.py
│   └── infer.py
├── data/
│   ├── raw/               ← not committed (large binary)
│   └── processed/         ← not committed (generated)
├── models/                ← not committed (large binary weights)
├── results/               ← commit eval_report.txt, not .pth files
└── docs/
    ├── AOI_PROJECT_STATE.md
    ├── AOI_DEBUG_EVOLUTION.md
    ├── README_phase1.md
    ├── DEBUG_LOG.md
    ├── REPO_CLEANUP_NOTE.md   ← this file
    └── archive/
        └── screenshots/   ← for Snipaste captures and other visual references
```

---

## Duplicate files

| File | Status | Recommended action |
|---|---|---|
| `DEBUG_LOG.md` (root) | Duplicate of `docs/DEBUG_LOG.md` — byte-for-byte identical. Root copy even contains a note pointing to `docs/` as the correct location. | Delete root copy. Keep `docs/DEBUG_LOG.md`. Do not delete automatically. |

---

## Files that should move

| File | Current location | Recommended destination | Reason |
|---|---|---|---|
| `Snipaste_2026-04-01_20-00-05.jpg` | repo root | `docs/archive/screenshots/` | Screenshot artifacts should not live at root. Keeping it in `docs/archive/screenshots/` makes the root clean and groups visual references together. |

---

## Files that should NOT be committed

These belong in `.gitignore` and should never be pushed:

| Path | Reason |
|---|---|
| `data/raw/LSWMD.pkl` | ~370 MB binary, Kaggle dataset |
| `data/processed/` | Generated from raw — can be regenerated by running `prepare_data.py` |
| `models/*.pth` | Large trained weights — consider separate model registry in Phase 2+ |
| `__pycache__/`, `*.pyc` | Python bytecode |
| `.env` | Would contain secrets if added in future phases |

---

## Docs files — current status

| File | Role | Keep? |
|---|---|---|
| `docs/AOI_PROJECT_STATE.md` | Phase tracking, locked JSON contract, roadmap | Yes |
| `docs/AOI_DEBUG_EVOLUTION.md` | Structured narrative: data bug → training → eval → inference | Yes |
| `docs/README_phase1.md` | Technical runbook for Phase 1 | Yes |
| `docs/DEBUG_LOG.md` | Granular dated engineering log with epoch-by-epoch data | Yes |
| `docs/AOI_PHASE1_APPROVED_PLAN.md` | Original planning approval record | Yes — archive value |
| `docs/AOI_CLAUDE.md` | AI collaboration instructions | Yes — internal use |
| `docs/AOI_COWORK_INSTRUCTIONS.md` | Collaboration rules | Yes — internal use |
| `docs/AOI_CHAT_INIT.md` | Session init prompt | Yes — internal use |
| `docs/AOI_CLI_START_PROMPT.md` | CLI start prompt | Yes — internal use |
| `docs/AOI_IMPLEMENTATION_NEXT_PROMPT.md` | Implementation prompt | Yes — internal use |

---

## Actions pending manual approval

- [ ] Delete `DEBUG_LOG.md` from repo root (confirmed duplicate)
- [ ] Move `Snipaste_2026-04-01_20-00-05.jpg` → `docs/archive/screenshots/`
- [ ] Create `docs/archive/screenshots/` folder if moving the screenshot
